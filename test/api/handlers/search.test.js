'use strict';

const path = '../../..';

const test = require('tape');
const serverFunction = require(path+'/api/server.js');

test('api:handler:search -> should return hello world', t => {

  // const server = serverFunction({});
  
  // console.log(serverFunction.toString());
  t.end();
});
